FROM python:3.10-slim

WORKDIR /app

COPY api/agent ./api_agent

RUN pip install --no-cache-dir fastapi uvicorn httpx langdetect prometheus_client

EXPOSE 8000

CMD ["uvicorn", "api_agent.main:app", "--host", "0.0.0.0", "--port", "8000"]
